# 更新与维护指南

本文档说明如何把本仓库的改动同步到 Gitee / GitHub 双平台，以及如何更新到 Windows / NAS / Termux 运行环境。

## 一、仓库地址（访问地址）

| 平台 | 地址 |
|------|------|
| Gitee | https://gitee.com/cloud_moon_yarn/ms-rewards-auto |
| GitHub | https://github.com/wenzongze/ms-rewards-auto |

**相关访问地址**（本工具无独立 Web 面板）：
- Microsoft Rewards 仪表盘：https://rewards.bing.com/
- 运行状态查看：Windows 任务计划程序历史 / NAS 容器日志 / Termux 终端输出
- 由图床项目的 Web 面板（端口 9999）可统一查看微软积分任务的运行情况（若已联动部署）

---

## 二、本地双远程配置

仓库克隆到本地后，已配置两个远程：

```powershell
git remote -v
# origin   -> https://gitee.com/cloud_moon_yarn/ms-rewards-auto (Gitee)
# github   -> https://github.com/wenzongze/ms-rewards-auto      (GitHub)
```

若你是从零克隆，可手动添加：

```powershell
git remote add origin  https://gitee.com/cloud_moon_yarn/ms-rewards-auto.git
git remote add github  https://github.com/wenzongze/ms-rewards-auto.git
```

---

## 三、日常更新代码并推送到双平台

修改代码后，在本地仓库目录执行：

```powershell
# 1. 暂存并提交
git add -A
git commit -m "描述你的改动"

# 2. 推送到 Gitee
git push origin master

# 3. 推送到 GitHub
git push github master
```

> 两个平台分支名均为 `master`。

---

## 四、把更新同步到各运行环境

### Windows
```powershell
cd <仓库目录>
git pull origin master
# 重新运行 install_task.ps1 或手动更新任务计划
```

### NAS（Docker）
```bash
cd <仓库目录>
git pull origin master        # 或 git pull github master
# 重新构建镜像并重启容器
docker build -t ms-rewards .
docker restart ms-rewards     # 或直接重新运行 nas_cron.sh
```
首次使用仍需先在 Windows 跑一次 `main.py` 登录，再把 `storage_state.json` 同步到 NAS。

### 手机 Termux
```bash
cd ~/ms-rewards-auto
git pull origin master
# 按《TERMUX_手机自动签到指南.md》重新配置定时任务
```

> ⚠️ 隐私文件（`config.json`、`storage_state.json`、`edge_profile/` 等）已在 `.gitignore` 排除，不会随 pull 覆盖你的本地登录态。

---

## 五、首次部署简要回顾

详见仓库内对应文档：
- 《微软积分自动获取-部署与图形化操作指南.md》 — NAS / Docker 部署
- 《TERMUX_手机自动签到指南.md》 — 安卓手机部署
- `config.example.json` 复制为 `config.json` 填好你的账号相关配置

---

## 六、常见问题

- **推送被拒绝？** 先 `git pull` 合并远端改动再 push。
- **登录态失效？** 重新在 Windows 跑 `main.py` 登录，更新 `storage_state.json`。
- **隐私泄露风险？** 切勿提交 `config.json`、`storage_state.json`、`edge_profile/`、`*.log`；它们已在 `.gitignore` 中。
- **账号风险？** 微软官方不允许自动化获取积分，请控制频率、仅在个人测试环境使用。
