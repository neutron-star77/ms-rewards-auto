# 微软积分自动获取（Microsoft Rewards）—— NAS 部署与图形化操作指南

> 适用环境：QNAP NAS（QTS 系统），通过 **Docker（Container Station）** 运行 Playwright + Chromium。
> 配套文件：`nas_main.py`（NAS 版主脚本）、`nas_cron.sh`（宿主机定时触发）、`Dockerfile`（镜像构建）、`config_nas.json`（配置）、`storage_state.json`（登录态，需自行生成）。
>
> **📌 路径说明**：本文档中所有 `/share/CACHEDEV1_DATA/...` 为 QNAP 常见示例路径，**请按你的 NAS 实际路径替换**（尤其是脚本部署目录与 docker/python 可执行文件路径）。
>
> **📌 关于图形化面板**：本项目的微软积分任务可由「图床自动扩容」项目里的 Web 面板（`web_app.py`）统一管理（同一 crontab 会被面板读取展示）。若你只用微软积分、不部署那个面板，可忽略文中面板相关内容，直接用 cron + docker 运行即可。

---

## 一、功能说明

`nas_main.py` 是一个基于 **Playwright + Chromium** 的自动化脚本，用于在 NAS 上无人值守地完成微软积分（Microsoft Rewards）的每日任务：

- **PC 端搜索**：完成当日必应 PC 搜索配额（默认 30 次）。
- **移动端搜索**：用移动 UA 模拟手机必应搜索（默认同样 30 次）。
- **每日任务卡片**：自动点击仪表盘/积分页的任务卡片（每日活动、Quiz 自动点「下一题」、拼图等）。
- **每日签到 / 打卡**：打开 Rewards 仪表盘识别并点击签到入口。
- **进度汇总**：运行结束打印当前可用积分与任务进度。

### 与 Windows 版的区别

| 项目 | Windows 版 (`main.py`) | NAS 版 (`nas_main.py`) |
|------|----------------------|------------------------|
| 浏览器 | 系统 Microsoft Edge | Playwright 自带 Chromium（headless） |
| 登录态 | `edge_profile` 持久化目录 | `storage_state.json` 注入 cookie |
| 运行方式 | 直接 Python | Docker 容器（推荐） |
| 显示 | 可有界面 | 强制无头（无显示器） |

> **关键前提**：NAS 端**不负责首次登录**。需先在 Windows 上跑 `main.py` 完成手动登录，把生成的 `storage_state.json` 同步到 NAS。cookie 过期后需重新在 Windows 登录并同步该文件。

### 文件清单与用途

| 文件 | 用途 |
|------|------|
| `nas_main.py` | NAS 版主脚本（登录态注入 + 各项任务；支持 `--keepalive` 保活模式） |
| `nas_cron.sh` | 宿主机 cron 调用的每日任务触发脚本（含随机延迟 + docker run） |
| `nas_keepalive.sh` | 会话保活脚本（每 18 天左右跑一次保活模式，延长登录态） |
| `Dockerfile` | 构建 `ms-rewards` 镜像 |
| `config_nas.json` | NAS 版配置（搜索次数、是否移动端等） |
| `storage_state.json` | 从 Windows 同步的登录态（**必需**） |
| `config.example.json` | 配置模板参考 |
| `requirements_nas.txt` | 容器依赖（playwright、rich） |
| `web_app.py` | 图形化面板（与图床共用，端口 9999） |

---

## 二、前置条件

1. **QNAP NAS** 已开启 SSH，已安装 **Container Station**（提供 `docker` 命令）。
   Docker 绝对路径：
   ```
   /share/CACHEDEV1_DATA/.qpkg/container-station/bin/docker
   ```
2. 一台 **Windows 电脑**用于首次登录并导出 `storage_state.json`。
3. NAS 上已部署图床面板（`web_app.py` 在 9999 端口运行），本任务作为面板中的一张卡片展示与操作。
4. NAS 目录约定：
   ```
   /share/CACHEDEV1_DATA/Web/gadget/microsoft   # 脚本与配置
   ```

---

## 三、部署到 NAS

### 步骤 1：在 Windows 上生成登录态

1. 在 Windows 上按 `microsoft/README.md` 配置并运行 `main.py`，**手动登录**一次微软账号。
2. 登录成功后，脚本会保存登录态。把下列文件从 Windows 拷到 NAS 的 `microsoft` 目录：
   - `storage_state.json`（核心登录态，必需）
3. （可选）把 `config.example.json` 复制为 `config_nas.json` 并填写：

```json
{
  "daily_search_count": 30,
  "use_mobile": true,
  "use_storage_state": true
}
```

### 步骤 2：上传文件到 NAS

将 `microsoft` 目录（含 `nas_main.py`、`nas_cron.sh`、`Dockerfile`、`requirements_nas.txt`、`config_nas.json`、`storage_state.json`）上传到：
```
/share/CACHEDEV1_DATA/Web/gadget/microsoft
```

### 步骤 3：构建 Docker 镜像

SSH 登录 NAS，进入目录并构建：
```bash
cd /share/CACHEDEV1_DATA/Web/gadget/microsoft
DOCKER=/share/CACHEDEV1_DATA/.qpkg/container-station/bin/docker
$DOCKER build -t ms-rewards /share/CACHEDEV1_DATA/Web/gadget/microsoft
```

构建会拉取 `python:3.11-slim` 并 `playwright install chromium`，耗时较长，请耐心等待。

验证镜像：
```bash
$DOCKER images | grep ms-rewards
```

### 步骤 4：手动测试一次

```bash
/share/CACHEDEV1_DATA/.qpkg/container-station/bin/docker run --rm \
  -v /share/CACHEDEV1_DATA/Web/gadget/microsoft:/app \
  ms-rewards python nas_main.py
```

观察日志：应显示「登录态有效」「PC 端搜索完成」「移动端搜索完成」「当前可用积分：XX,XXX」。日志同时写入 `microsoft/nas.log`。

> 若提示「未登录 / storage_state.json 已失效」，回到 Windows 重新登录并重新同步 `storage_state.json`。

### 步骤 5：配置定时任务（cron）

`nas_cron.sh` 已内置**随机延迟 0~30 分钟**（避免固定时间被检测）。加入 crontab（每天 09:00 与 21:00 触发）：

```bash
cp /etc/config/crontab /etc/config/crontab.bak
vi /etc/config/crontab
```

追加：
```
0 9,21 * * * /bin/sh /share/CACHEDEV1_DATA/Web/gadget/microsoft/nas_cron.sh >> /share/CACHEDEV1_DATA/Web/gadget/microsoft/nas.log 2>&1
```

重载 cron：
```bash
/etc/init.d/crond.sh restart
```

`nas_cron.sh` 内部逻辑：
1. 随机延迟 `0~1800` 秒（`$((RANDOM % 1800))`）。
2. 用绝对路径的 `docker` 启动一次性容器：`docker run --rm -v ...:/app ms-rewards python nas_main.py`。
3. 输出「本次运行结束」到 `nas.log`。

> 注意 `nas_cron.sh` 中已写死 `DOCKER_BIN` 绝对路径与 `SCRIPT_DIR`，因为 **cron 环境 PATH 很精简**，直接用 `docker` 会找不到命令。

---

## 四、图形化面板操作详解（与图床共用面板）

微软积分任务作为面板中「⭐ 我的自动执行任务」里的一张卡片展示。打开 `http://<NAS_IP>:9999`：

```
📋 全部计划任务
⭐ 我的自动执行任务
  ┌─ 任务卡片 ───────────────────────────────┐
  │ 🪟 微软积分自动获取  [自动] [0 9,21 * * *] │
  │ 说明：每天 09:00 与 21:00 触发，内部随机   │
  │       延迟 0~30 分钟…                       │
  │ [▶ 立即执行]  [📜 日志]                    │
  │ ┌─ 运行日志区（nas.log 最近 200 行）─┐     │
  └────────────────────────────────────┘
```

### 1. 立即执行（手动触发）

- 点 `▶ 立即执行` → 调用 `/api/run-job`，以**手动模式**触发 `nas_cron.sh`（含内部随机延迟 0~30 分钟）。
- 执行中按钮显示 `⏳ 执行中...`。
- 完成后自动展开日志区，可看到搜索进度与最终积分。

> 说明：微软任务的「立即执行」仍走 `nas_cron.sh`，因此**会包含 0~30 分钟随机延迟**（脚本设计如此，模拟真人）。日志需等待延迟结束后才有内容。

### 2. 查看运行日志

- 点 `📜 日志` → 读取 `microsoft/nas.log` 最近 200 行，显示：
  - 随机延迟秒数
  - 登录态是否有效
  - PC / 移动端搜索进度 `[i/N]`
  - 任务卡片完成情况
  - 当前可用积分
- 再次点 `📜 日志` 收起。

### 3. 暂停 / 启用、刷新、重启面板、改标题、悬浮按钮

均与「图床自动扩容」面板一致（见图床文档第四节），此处不再重复。要点：
- `⏸ 暂停计划任务`：影响的是图床脚本的 `disabled.flag`；微软任务由 cron 独立调度，暂停面板不影响微软 cron（如需暂停微软，注释掉 crontab 中对应行或用 `crond.sh restart`）。
- `♻️ 重启面板`：仅重启 `web_app.py`，不影响微软 cron 与 Docker。
- 右下角 `🌙/☀️` 切换主题、`⬆️` 回顶（向下滚隐藏、向上滚出现）。

---

## 五、登录态保活（延长 cookie、减少手动重登）

### 背景

微软账户登录 cookie **无法被"延长"到期时间**（由服务端控制），且**你已开启 2FA**，一旦被强制重新认证就**必须人工输入验证码**——无法全自动。因此目标是：**尽量让会话保持活跃、顺延有效期，把手动重登频率压到最低（理想数月一次）**。

实际经验：微软网页登录态常态可维持约 **30~60 天**；触发提前失效的主因是**长期不活动**和**风控判定异常登录**（IP/UA 突变、自动化特征）。NAS 用 Playwright 跑本身较易被判异常，所以更需要"低调 + 保活"。

### 方案：轻量保活模式

`nas_main.py` 新增 `--keepalive` 保活模式：**只验证登录态 + 访问几个已登录页面（dashboard 等），不跑搜索任务**，运行结束把顺延后的 cookie **回写 `storage_state.json`**。作用是定期"刷存在感"，维持会话活跃。

> 注意：每次**正常任务**（`run()`）结束现在也会自动回写 `storage_state.json`，因此日常跑任务本身就有一定保活效果。`--keepalive` 是给"某段时间没跑任务"时的额外兜底。

### 手动测试保活

```bash
/share/CACHEDEV1_DATA/.qpkg/container-station/bin/docker run --rm \
  -v /share/CACHEDEV1_DATA/Web/gadget/microsoft:/app \
  ms-rewards python nas_main.py --keepalive
```

应看到「登录态有效」→ 依次访问 dashboard/rewards/bing → 打印积分 → 「已回写登录态」→「保活完成」。日志写入 `microsoft/keepalive.log`。

### 配置保活定时任务

用 `nas_keepalive.sh`（已内置小范围随机延迟 0~5 分钟）。加入 crontab，建议**每 18 天左右一次、与每日任务错开**：

```bash
cp /etc/config/crontab /etc/config/crontab.bak
vi /etc/config/crontab
```

追加：
```
30 3 */18 * * /bin/sh /share/CACHEDEV1_DATA/Web/gadget/microsoft/nas_keepalive.sh >> /share/CACHEDEV1_DATA/Web/gadget/microsoft/keepalive.log 2>&1
```

重载：
```bash
/etc/init.d/crond.sh restart
```

### 进一步降低被强制重认证的概率

- **固定 UA**：不要频繁改 `nas_main.py` 里的 UA 字符串。
- **固定出口 IP**：NAS 网络别频繁切换/走代理跳变。
- **别频繁手动「立即执行」**：每天 2 次自动 + 偶尔手动即可，跑太勤更易触发风控。
- **保持每日活动**：让每日任务稳定运行，会话持续活跃更不易过期。

### 仍需人工的唯一情况

当日志出现 **`登录态已失效，需在 Windows 重新登录并同步 storage_state.json`**（保活/任务都检测到未登录）时，说明微软已强制重认证。由于开了 2FA，此时**必须**：
1. 在 Windows 跑 `main.py` 手动登录（过 2FA）。
2. 把新的 `storage_state.json` 同步回 NAS `microsoft` 目录。
3. 下次 cron 或手动执行即恢复自动。

---

## 六、维护与排错

| 现象 | 原因 / 解决 |
|------|------------|
| 日志显示「未登录 / storage_state 失效」 | cookie 过期。回 Windows 跑 `main.py` 重新登录，把新 `storage_state.json` 同步到 NAS |
| cron 到点没跑 | 确认 crontab 已写入并 `crond.sh restart`；`nas_cron.sh` 中 `DOCKER_BIN` 路径正确；`docker images` 有 `ms-rewards` |
| docker: command not found | cron 环境 PATH 不含 docker；`nas_cron.sh` 已用绝对路径，确认路径 `/share/CACHEDEV1_DATA/.qpkg/container-station/bin/docker` 存在 |
| 容器启动即退 | 查看 `nas.log`；多为 `storage_state.json` 缺失或 `config_nas.json` 格式错误 |
| 积分不见增长 | 微软可能风控；降低 `daily_search_count`、拉长随机延迟；勿频繁手动执行 |
| 面板看不到微软卡片 | 确认 crontab 中 `nas_cron.sh` 行存在；`curl http://127.0.0.1:9999/api/status` 看 `cron_jobs` 是否含微软项 |

### 重新构建镜像（脚本更新后）

```bash
cd /share/CACHEDEV1_DATA/Web/gadget/microsoft
DOCKER=/share/CACHEDEV1_DATA/.qpkg/container-station/bin/docker
$DOCKER build -t ms-rewards /share/CACHEDEV1_DATA/Web/gadget/microsoft
```

---

## 七、风险提示

> ⚠️ 微软官方不允许使用自动化工具获取积分，使用本脚本存在账户被警告或封禁的风险。请自行评估并控制运行频率（默认每天 2 次、含随机延迟已较保守）。仅建议在个人环境使用，因使用本脚本导致的任何后果由使用者自行承担。

---

## 八、目录与路径速查

| 项目 | 路径 |
|------|------|
| 脚本目录 | `/share/CACHEDEV1_DATA/Web/gadget/microsoft` |
| 每日触发脚本 | `/share/CACHEDEV1_DATA/Web/gadget/microsoft/nas_cron.sh` |
| 保活脚本 | `/share/CACHEDEV1_DATA/Web/gadget/microsoft/nas_keepalive.sh` |
| 运行日志 | `/share/CACHEDEV1_DATA/Web/gadget/microsoft/nas.log` |
| 保活日志 | `/share/CACHEDEV1_DATA/Web/gadget/microsoft/keepalive.log` |
| 登录态 | `/share/CACHEDEV1_DATA/Web/gadget/microsoft/storage_state.json` |
| Docker | `/share/CACHEDEV1_DATA/.qpkg/container-station/bin/docker` |
| 镜像名 | `ms-rewards` |
| 面板地址 | `http://<NAS_IP>:9999` |
